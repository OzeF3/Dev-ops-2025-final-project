apiVersion: v1
kind: Service
metadata:
  name: seyoawe-engine
spec:
  selector:
    app: seyoawe-engine
  ports:
    - protocol: TCP
      port: 8080 ## replaced to 8080 from 5000
      targetPort: 8080 ## replaced to 8080 from 5000
  type: ClusterIP