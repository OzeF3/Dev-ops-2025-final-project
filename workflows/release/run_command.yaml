id: run_command
name: "Run Command"
description: "Runs a shell command on the server and returns the output"

trigger:
  type: api

steps:
  - id: run_command
    type: command
    config:
      command: "{{ context.trigger.payload.command }}"
      timeout: 30
